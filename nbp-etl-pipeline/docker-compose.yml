version: '3.8'

services:
  db:
    image: postgres:15
    container_name: nbp_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: POSTGRES_PASSWORD
      POSTGRES_DB: nbp_data
    ports:
      - "5432:5432"